# StudyBridge MSA — Kubernetes 배포 발표

---

## 1. 프로젝트 소개

**StudyBridge**는 스터디 모집·신청·알림을 제공하는 플랫폼입니다.  
단일 서버(Monolith)로 시작한 프로젝트를 **마이크로서비스 아키텍처(MSA)** 로 전환하고  
**Kubernetes** 위에 배포하는 것이 이번 발표의 핵심 주제입니다.

### 주요 기능
| 기능 | 설명 |
|------|------|
| 스터디 모집 | 모집글 등록·조회·수정·삭제 |
| 스터디 신청 | 신청서 작성 및 상태 관리 |
| 댓글 | 모집글별 댓글 작성·수정·삭제 |
| 실시간 알림 | 신청 발생 시 SSE 푸시 알림 |

---

## 2. 전체 MSA 아키텍처

```
[ 브라우저 ]
     │  http://localhost:30000
     ▼
[ frontend (React/Nginx) ]   NodePort: 30000
     │  /api/**, /notifications/**
     ▼
[ gateway-service ]          NodePort: 30080
  Spring Cloud Gateway
  ├── /api/studies/**    → study-service:8081
  ├── /api/applications/** → apply-service:8082
  ├── /api/comments/**   → comment-service:8083
  ├── /api/notifications/** → notification-service:8085
  └── /client/**         → client-service:8084

[ apply-service ] ──RabbitMQ──▶ [ notification-service ]
                  이벤트 발행          SSE 알림 전송
```

### 서비스 목록
| 서비스 | 기술 | 포트 |
|--------|------|------|
| frontend | React + Nginx | 80 |
| gateway-service | Spring Cloud Gateway | 8080 |
| study-service | Spring Boot + JPA | 8081 |
| apply-service | Spring Boot + RabbitMQ | 8082 |
| comment-service | Spring Boot + JPA | 8083 |
| client-service | Spring Boot + OpenFeign | 8084 |
| notification-service | Spring Boot + SSE | 8085 |
| MySQL | mysql:8.0 | 3306 |
| RabbitMQ | rabbitmq:3-management | 5672 / 15672 |

---

## 3. Kubernetes 배포 구성

### 네임스페이스
모든 리소스를 `studybridge` 네임스페이스로 격리합니다.

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: studybridge
```

### 서비스 노출 방식

| 리소스 | 유형 | 외부 접근 포트 |
|--------|------|--------------|
| frontend | NodePort | 30000 |
| gateway-service | NodePort | 30080 |
| MySQL (외부 접근) | NodePort | 30307 |
| RabbitMQ AMQP | NodePort | 30672 |
| RabbitMQ 관리UI | NodePort | 31672 |
| 내부 서비스 통신 | ClusterIP | (내부 전용) |

### imagePullPolicy 설정
로컬에서 빌드한 이미지를 사용하기 위해 `IfNotPresent` 설정:

```yaml
containers:
  - name: study-service
    image: kanghyeongu00/studybridge-study-service:latest
    imagePullPolicy: IfNotPresent
```

---

## 4. PV/PVC & StatefulSet

### 왜 StatefulSet인가?

| 항목 | Deployment | StatefulSet |
|------|-----------|-------------|
| Pod 이름 | 랜덤 (mysql-7d9f-xxx) | 고정 (mysql-0) |
| 스토리지 | 공유 PVC | Pod별 PVC 자동 생성 |
| 재시작 순서 | 무작위 | 순서 보장 |
| 용도 | 무상태 앱 | **DB처럼 상태 있는 앱** |

→ MySQL, RabbitMQ는 데이터 유실 없이 안정적으로 운영하기 위해 **StatefulSet** 사용

### Headless Service

StatefulSet과 함께 쓰는 `clusterIP: None` 서비스.  
`mysql-0.mysql.studybridge.svc.cluster.local` 처럼 **특정 Pod로 직접 DNS 접근** 가능.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: studybridge
spec:
  clusterIP: None       # Headless
  selector:
    app: mysql
  ports:
    - port: 3306
```

### volumeClaimTemplates

StatefulSet은 Pod별로 PVC를 자동 생성합니다.  
MySQL → `mysql-data-mysql-0` PVC 자동 생성 (2Gi)

```yaml
volumeClaimTemplates:
  - metadata:
      name: mysql-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 2Gi
```

Docker Desktop의 기본 StorageClass(`docker.io/hostpath`)가  
PVC 요청을 받아 **자동으로 PV를 생성(Dynamic Provisioning)** 합니다.

```
PVC 요청 → StorageClass → PV 자동 생성 → Pod에 마운트
```

---

## 5. RabbitMQ 이벤트 기반 알림

스터디 신청 시 알림이 전달되는 흐름:

```
사용자
  │ POST /api/applications
  ▼
apply-service
  │ DB 저장 후 ApplicationEvent 발행
  ▼
RabbitMQ Exchange (studybridge.exchange)
  │ routing-key: application.created
  ▼
studybridge.application.queue
  │
  ▼
notification-service
  │ 메시지 수신 → Notification DB 저장
  ▼
SSE (Server-Sent Events)
  │
  ▼
브라우저 알림창 실시간 표시
```

### 핵심 코드 (apply-service)
```java
@PostMapping
StudyApplication create(@RequestBody StudyApplication application) {
    application.id = null;              // DB AUTO_INCREMENT 보장
    application.status = "신청 완료";
    application.createdAt = LocalDateTime.now();
    StudyApplication saved = repository.save(application);
    publish(saved);                     // RabbitMQ 이벤트 발행
    return saved;
}
```

---

## 6. Istio 서비스 메시

### 구성
- **Istio**: 서비스 간 트래픽을 제어하는 서비스 메시
- **Envoy Sidecar**: 각 Pod에 자동 주입되어 모든 트래픽을 가로챔
- **Kiali**: 서비스 메시 시각화 대시보드

### 적용 방법

```bash
# 네임스페이스에 Istio 사이드카 자동 주입 활성화
kubectl label namespace studybridge istio-injection=enabled

# Pod 재시작 → 모든 Pod에 Envoy 사이드카 주입
kubectl rollout restart deployment -n studybridge
```

### 효과
- Pod당 컨테이너가 `1/1` → `2/2` 로 변경 (앱 + Envoy)
- Kiali 대시보드에서 서비스 간 트래픽 흐름 실시간 시각화
- 별도 코드 수정 없이 **mTLS, 트래픽 관찰, 재시도 정책** 적용 가능

---

## 7. 모니터링 스택

### 구성도

```
[ Spring Boot 서비스들 ]
  /actuator/prometheus 엔드포인트 노출
          │
          ▼ scrape (15초 간격)
    [ Prometheus ]  ← NodePort: 30090
          │
          ▼ 데이터 소스
      [ Grafana ]   ← NodePort: 30300
          ▲
          │ 데이터 소스
      [ Loki ]      ← 로그 집계
          ▲
          │ 로그 수집
     [ Promtail ]   ← DaemonSet (모든 노드)
          │ /var/log/pods 마운트
     [ K8s Pod 로그 ]
```

### Prometheus 메트릭 수집

Spring Boot 서비스에 Micrometer 의존성 추가:

```xml
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
```

```properties
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.metrics.tags.application=study-service
```

### Loki + Promtail 로그 수집

- **Promtail**: DaemonSet으로 배포 → 노드의 `/var/log/pods` 를 마운트하여 모든 Pod 로그 수집
- **Loki**: 로그 집계 및 저장
- **Grafana Explore**: `{app="study-service"}` 쿼리로 실시간 로그 조회

### Grafana 대시보드

| 대시보드 | 내용 |
|---------|------|
| Spring Boot JVM (ID: 4701) | JVM Heap, GC, HTTP 요청수, 응답시간 |
| Loki Explore | 서비스별 실시간 로그 조회 |

---

## 8. 배포 명령어 요약

```bash
# 전체 서비스 배포
kubectl apply -f k8s/studybridge-all.yaml

# 모니터링 스택 배포
kubectl apply -f k8s/monitoring.yaml

# Pod 상태 확인
kubectl get pod -n studybridge

# 실시간 로그 확인
kubectl logs -f -n studybridge deployment/study-service

# 서비스 재시작
kubectl rollout restart deployment -n studybridge
```

---

## 9. 접속 URL 정리

| 서비스 | URL |
|--------|-----|
| 프론트엔드 | http://localhost:30000 |
| API Gateway | http://localhost:30080 |
| Grafana | http://localhost:30300 (admin / admin123) |
| Prometheus | http://localhost:30090 |
| RabbitMQ 관리 UI | http://localhost:31672 (admin / admin123) |

---

## 10. 핵심 학습 포인트

| 주제 | 내용 |
|------|------|
| MSA 분리 | 기능별 독립 서비스로 분리 → 독립 배포·확장 가능 |
| Kubernetes | Deployment, StatefulSet, Service, PVC로 컨테이너 오케스트레이션 |
| PV/PVC | Dynamic Provisioning으로 DB 데이터 영속성 보장 |
| StatefulSet | DB 같은 상태 있는 앱에 적합한 워크로드 |
| Headless Service | StatefulSet Pod에 DNS 직접 접근 |
| Istio | 코드 변경 없이 서비스 메시 적용 |
| 모니터링 | Prometheus(메트릭) + Loki(로그) + Grafana(시각화) |
| 메시지 큐 | RabbitMQ로 서비스 간 느슨한 결합 실현 |
