# StudyBridge v2 MSA - 코드 분석 결과

## 한줄 요약
교수님 MSA 강의 예제를 **대학생 스터디 모집 플랫폼**으로 변환한 기말 프로젝트

---

## 1. 전체 아키텍처

```
Frontend (React:3000)
    ↓ Nginx proxy
Gateway Service (:8080)  ← 진입점
    ↓ 라우팅
┌───────────────────────────────────────┐
│ study-service   (:8081) - 스터디 CRUD │
│ apply-service   (:8082) - 신청 처리   │
│ comment-service (:8083) - 댓글 관리   │
│ notification    (:8085) - 알림 처리   │
│ client-service  (:8084) - API 호출예시│
└──────────────┬────────────────────────┘
               ↓
          MySQL (:3306) / RabbitMQ (:5672)
```

---

## 2. 서비스별 역할 및 기능

### study-service (포트: 8081)
- 스터디 모집글 CRUD
- `GET /api/studies` - 전체 목록
- `GET /api/studies/{id}` - 상세 조회
- `POST /api/studies` - 등록
- `PUT /api/studies/{id}` - 수정
- `DELETE /api/studies/{id}` - 삭제

### apply-service (포트: 8082)
- 스터디 신청 처리 + RabbitMQ 이벤트 발행
- `GET /api/applications` - 전체 신청 목록
- `GET /api/applications/study/{studyId}` - 스터디별 신청 목록
- `GET /api/applications/applicant/{name}` - 지원자별 신청 목록
- `POST /api/applications` - 신청 생성
- `PUT /api/applications/{id}/status` - 상태 변경 (수락/거절 등)

### comment-service (포트: 8083)
- 스터디 상세 페이지 댓글 관리
- `GET /api/comments/study/{studyId}` - 댓글 조회
- `POST /api/comments` - 댓글 작성
- `PUT /api/comments/{id}` - 댓글 수정
- `DELETE /api/comments/{id}` - 댓글 삭제

### notification-service (포트: 8085)
- RabbitMQ 이벤트 수신 → 알림 저장
- WebFlux SSE로 실시간 알림 스트림 제공
- `GET /api/notifications` - 최근 20개 알림
- `PUT /api/notifications/read-all` - 전체 읽음 처리
- `GET /notifications/flux` - SSE 실시간 스트림

### client-service (포트: 8084)
- RestTemplate vs OpenFeign 비교 시연용
- `GET /client/resttemplate/studies` - RestTemplate 호출
- `GET /client/openfeign/studies` - OpenFeign 호출
- `GET /client/compare` - 두 방식 비교

### gateway-service (포트: 8080)
- 모든 서비스 라우팅 + CORS 처리
- `/api/studies/**` → study-service:8081
- `/api/comments/**` → comment-service:8083
- `/api/applications/**` → apply-service:8082
- `/api/notifications/**` → notification-service:8085
- `/notifications/flux` → notification-service:8085 (SSE)
- `/client/**` → client-service:8084
- CORS 허용: localhost:3000, localhost:5173

### frontend (포트: 3000)
- React + TypeScript 기반 UI
- `/studies` - 스터디 목록 및 필터
- `/create` - 모집글 등록
- `/studies/:id` - 상세 정보 + 댓글
- `/my` - 내 신청/모집 현황
- `/activity` - 신청 현황 및 알림
- `/edit/:id` - 모집글 수정
- `/architecture` - 아키텍처 설명

---

## 3. 기술 스택

### 백엔드 (공통)
- Java 17
- Spring Boot 3.4.2
- Spring Data JPA
- MySQL Connector
- Maven (빌드)
- Docker 멀티 스테이지 빌드

### 서비스별 추가 의존성
| 서비스 | 추가 의존성 |
|--------|------------|
| apply-service | spring-boot-starter-amqp (RabbitMQ) |
| notification-service | spring-boot-starter-amqp, spring-boot-starter-webflux |
| client-service | spring-cloud-starter-openfeign, Spring Cloud 2024.0.0 |
| gateway-service | spring-cloud-starter-gateway, spring-boot-starter-webflux, Spring Cloud 2024.0.0 |

### 프론트엔드
- React 19.1.0
- TypeScript 5.8.3
- Vite 6.3.5
- Tailwind CSS 4.1.8
- React Hook Form 7.55.0
- TanStack React Query 5.83.0
- Wouter (라우팅)
- Nginx 1.27-alpine (프로덕션 서빙)

---

## 4. DB 연동 현황

**상태: 연동 완료**

- DB: MySQL 8.0 (`studybridge` 데이터베이스)
- `spring.jpa.hibernate.ddl-auto=update` (자동 테이블 생성)

| 서비스 | 테이블 |
|--------|--------|
| study-service | studies |
| apply-service | applications |
| comment-service | comments |
| notification-service | notifications |

---

## 5. 외부 API 연동 현황

**상태: 없음**

외부 API(카카오 로그인, 구글 OAuth, 이메일, 결제 등) 연동 없음.
`advanced/` 폴더에 Keycloak 인증 확장안만 별도 준비되어 있음.

---

## 6. 서비스 간 통신 방식

| 방식 | 사용처 | 특징 |
|------|--------|------|
| REST HTTP | Gateway → 각 서비스 | 동기식 |
| RabbitMQ | apply-service → notification-service | 비동기, 신청 이벤트 전달 |
| RestTemplate | client-service → study-service | 동기식 HTTP |
| OpenFeign | client-service → study-service | 선언적 REST 클라이언트 |
| WebFlux SSE | notification-service → 브라우저 | 실시간 푸시 스트림 |

### RabbitMQ 설정
- Exchange: `studybridge.exchange`
- Routing Key: `application.created`
- Queue: `studybridge.application.queue`

---

## 7. 주요 도메인 모델

### Study
```
id, title, field, recruiterName, recruiterId
capacity, currentCount
mode (온라인/오프라인/온·오프라인 병행)
status (모집 중/모집 마감)
level (입문/초급/중급/고급/자율)
schedule, period, location, deadline
description, purpose, requirements, materials, goals, tags
```

### StudyApplication
```
id, studyId, studyTitle, applicantId, applicantName
message, availableTime, level
status (신청 완료/검토 중/수락됨/거절됨/취소됨)
createdAt, decidedAt
```

### StudyComment
```
id, studyId, authorName, content
createdAt, updatedAt
```

### Notification
```
id, applicationId, studyId, studyTitle, applicantName
status, message
createdAt, readAt
```

---

## 8. 인프라 구성

### Docker Compose 서비스
| 서비스 | 이미지/빌드 | 포트 | 의존성 |
|--------|------------|------|--------|
| mysql | mysql:8.0 | 3307:3306 | - |
| rabbitmq | rabbitmq:3-management | 5672, 15672 | - |
| study-service | ./study-service 빌드 | 8081 | mysql(healthy) |
| comment-service | ./comment-service 빌드 | 8083 | mysql(healthy) |
| apply-service | ./apply-service 빌드 | 8082 | mysql(healthy), rabbitmq |
| notification-service | ./notification-service 빌드 | 8085 | mysql(healthy), rabbitmq |
| client-service | ./client-service 빌드 | 8084 | study-service |
| gateway-service | ./gateway-service 빌드 | 8080 | 모든 서비스 |
| frontend | ./frontend 빌드 | 3000:80 | gateway-service |

### 추가 인프라 파일 (k8s/, argocd/)
- Kubernetes 매니페스트 (PV/PVC 포함)
- ArgoCD GitOps 설정
- Istio Service Mesh 설정

---

## 9. 접속 정보 요약

| 주소 | 설명 | 계정 |
|------|------|------|
| http://localhost:3000 | 메인 웹사이트 | - |
| http://localhost:8080 | Gateway API | - |
| http://localhost:15672 | RabbitMQ 관리 콘솔 | admin / admin123 |
| localhost:3307 | MySQL | root / root1234 |

---

## 10. advanced/ 확장 학습 구조

- **Keycloak**: OAuth2/OIDC 인증 서버
- **Kafka + Debezium CDC**: 변경 데이터 캡처
- **Saga 패턴**: 분산 트랜잭션 보상 처리
- **Istio Service Mesh**: 트래픽 관리, 보안

---

*분석일: 2026-06-10*
